apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: electro-ingress
spec:
  rules:
  - host: electromarket.us.to
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: edge-service
            port:
              number: 80
